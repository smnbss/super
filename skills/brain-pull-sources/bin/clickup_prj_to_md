#!/bin/bash
# ClickUp Project/Folder to Markdown exporter wrapper
# Usage: clickup_prj_to_md <clickup_view_url>

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

python3 "$PROJECT_ROOT/utils/clickup_prj_to_md.py" "$@"
