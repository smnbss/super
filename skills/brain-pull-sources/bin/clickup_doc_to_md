#!/bin/bash
# ClickUp Document to Markdown exporter wrapper
# Usage: clickup_doc_to_md <clickup_doc_url>

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

python3 "$PROJECT_ROOT/utils/clickup_doc_to_md.py" "$@"
