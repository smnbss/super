{
  "name": "super-cli",
  "version": "5.8.10",
  "description": "Cross-CLI session bridge for Claude Code, Gemini CLI, Codex CLI",
  "type": "module",
  "bin": {
    "super": "./super"
  },
  "scripts": {
    "test": "for t in tests/test_*.mjs; do echo \"\n▸ $t\"; node \"$t\" || exit 1; done"
  },
  "dependencies": {
    "chrome-devtools-mcp": "^0.21.0",
    "js-yaml": "^4.1.0"
  }
}
